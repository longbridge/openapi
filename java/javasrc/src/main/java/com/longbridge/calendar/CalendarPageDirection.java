package com.longbridge.calendar;

/** Pagination direction for {@link CalendarContext#getFinanceCalendar} (the {@code next} parameter). */
public enum CalendarPageDirection {
    /** Page towards later dates */
    Later,
    /** Page towards earlier dates */
    Earlier,
}
